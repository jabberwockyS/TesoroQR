@model IEnumerable<TesoroQR.Models.Tesoro.Pista>

@{
    ViewBag.Title = "Pistas ";
}

<h2>Gestion de Pistas del camino...</h2>

<p>
    @Html.ActionLink("Agregar PIsta", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.orden)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.orden)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.PistaID }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.PistaID }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.PistaID })
        </td>
    </tr>
}

</table>
